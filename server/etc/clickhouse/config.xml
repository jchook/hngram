<?xml version="1.0"?>
<clickhouse>
    <!-- Low-memory configuration for small VPS (per RFC-007) -->

    <!-- Limit memory usage -->
    <max_server_memory_usage_to_ram_ratio>0.8</max_server_memory_usage_to_ram_ratio>
    <max_memory_usage>2000000000</max_memory_usage> <!-- 2GB -->

    <!-- Reduce background pool sizes -->
    <background_pool_size>2</background_pool_size>
    <background_schedule_pool_size>2</background_schedule_pool_size>
    <background_fetches_pool_size>2</background_fetches_pool_size>
    <background_move_pool_size>1</background_move_pool_size>

    <!-- Smaller caches -->
    <mark_cache_size>134217728</mark_cache_size> <!-- 128MB -->
    <uncompressed_cache_size>134217728</uncompressed_cache_size> <!-- 128MB -->

    <!-- Limit concurrent queries -->
    <max_concurrent_queries>20</max_concurrent_queries>

    <!-- Logging -->
    <logger>
        <level>warning</level>
        <console>true</console>
    </logger>
</clickhouse>
