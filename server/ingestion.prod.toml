# Production pruning thresholds (matches hardcoded defaults).
#
# Usage: cargo run -p ingestion -- process --output parquet --config ingestion.prod.toml

[pruning.1]
min_global = 0
min_bucket = 1

[pruning.2]
min_global = 20
min_bucket = 3

[pruning.3]
min_global = 10
min_bucket = 5
